.rating {
  display: flex;
  align-items: center;
  gap: 4px;
}

.star {
  display: block;
  [class^="icon-"], [class*=" icon-"] {
    display: block;
    fill: var(--badges);
  }
}

.active {
  [class^="icon-"], [class*=" icon-"] {
    fill: var(--rating);
  }
}