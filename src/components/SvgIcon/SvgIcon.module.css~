.icon {
  flex-shrink: 0;
  display: block;
  stroke-width: 0;
}

.icon[data-type="fill"] use {
  fill: currentColor;
  stroke: none;
}

.icon[data-type="stroke"] use {
  fill: none;
  stroke: currentColor;
}